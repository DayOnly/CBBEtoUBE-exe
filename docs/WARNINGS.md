# Every warning the converter can print

Generated by `python scripts/warning_surface.py` from the `warn(...)` calls in
the batch runner and the mesh writer; `tests/test_user_warnings.py` keeps it
current. A `!!` line is a problem; a `NOTE:` line is information. Each entry
gives the line as it appears in the run log (`…` stands for the value printed
at run time), what it means for the run, and what to do next.

**67** problem warnings and **2** notes.

## src/auto_convert.py

- `!! …: conversion raised`
  - means: the traceback follows; the piece is counted as an error
- `!! warm-up task failed: …`
  - means: the first real piece on that worker pays the cold start
- `!! … body-ref candidates -- under …`
  - means: scanning only the first 1500 by priority -- a UBE body in a deeply-nested non-'ube' path could be missed
  - fix: set the UBE body reference explicitly if the wrong body is picked
- `NOTE: … option(s) were added to this build since your settings were last saved.`
- `NOTE: your saved settings predate new-option tracking, so an option added later`
- `!! could not write the failures file (…)`
  - means: … recorded failure(s) will not appear there
  - fix: check that the folder beside the exe is writable
- `!! SkyPatcher: …`
  - means: SkyPatcher delivers ALL converted armor -- there is no ESP fallback. Without it every converted piece is INVISIBLE in-game.
  - fix: … (Converting anyway: the output stays valid, no reconvert needed once SkyPatcher is in place.)
- `!! refusing traversal output path for "…"`
  - means: the source names a path outside the output mod; the file was skipped
- `!! post-conversion load check skipped -- pynifly unavailable (…)`
  - means: output was NOT re-loaded or verified
- `!! could not write the conversion_report.json checkpoint -- under …`
  - means: a run that dies now leaves no report
  - fix: check that the output folder is writable and not open elsewhere
- `!! could not create the output folder: … -- …`
  - means: nothing can be written there
  - fix: check the path and its permissions
- `!! could not write conversion_settings.json -- under …`
  - means: the output mod will not record which build and settings made it
- `!! [unified] … of … 'already UBE' armors come from a single mod (…)`
  - means: if that is converter output rather than a hand-made patch, coverage is being suppressed wrongly
  - fix: check that mod before trusting this run
- `!! … coverage validator: … warning(s)`
  - means: the lines below name what it found in the generated race coverage; read them before trusting this run's coverage
- `!! [unified] could not scan for existing UBE patches (…)`
  - means: not excluding any
- `!! unified coverage emission failed: …`
  - means: continuing with per-source coverage
- `!! could not sweep orphaned temp files: … -- under …`
  - means: partial files from an interrupted run may remain
- `!! removed … orphaned temp file(s) that an interrupted run left -- under … (e.g. …)`
  - means: those were partial writes a killed run never finished; nothing of yours was touched
- `!! pre-warm failed (non-fatal): …`
  - means: the first pieces pay the cold start
- `!! incremental floor calc failed: …`
  - means: doing a full convert instead
- `!! no MO2 mods folder: cannot check other mods for existing UBE patches`
  - means: everything is converted, including armour another mod already patched
  - fix: run from inside the modlist, or point the Paths tab at its ModOrganizer.ini
- `!! could not scan for existing UBE patches (…)`
  - means: converting everything, including armour another mod already patched
- `!! vanilla sweep failed (…)`
  - means: retrying SERIALLY (no worker pool; slower, but immune to pool-environment failures)...
- `!! conversion failed: …`
  - means: the run stopped; the log above says where
  - fix: fix the cause and run again
- `!! VANILLA SWEEP FAILED: …`
  - means: vanilla armour that no mod overrides is not linked this run, so it is invisible on UBE actors
- `!! FAILED: …`
  - means: this source did not convert; nothing of it is in the output
- `!! CONVERSION ERRORS on … NIF(s):`
  - means: each mesh below did not convert and is absent from the output
- `!! LOAD FAILURES on … output NIFs`
  - means: the files below were written but cannot be read back; they will not load in game
- `!! POSTFLIGHT NIF (CTD-class): … invariant issue(s)`
  - means: a zero-vert or over-cap partition crashes the game on equip; the run is reported as failed; listed below
  - fix: do not ship those meshes
- `!! VirtualBody re-hide: … NIF(s) may show a visible body-double`
  - means: listed below
- `!! ESP GENERATION FAILED for … source ESP(s)`
  - means: their armor is ABSENT from the merge (invisible in-game): …
  - fix: read the reason beside each plugin and fix the source, then run again
- `!! PARTIAL: … NIF(s) dropped a shape`
  - means: an invisible piece in-game
  - fix: see the coverage report's PARTIAL section
- `!! PATCH VALIDATOR: … warning(s)`
  - means: listed below; the patch still loads, but check each before trusting it
- `!! POSTFLIGHT weight-partners: … body mesh(es) missing a _0/_1 partner`
  - means: invisible at one body weight; listed below
- `!! postflight weight-partner scan skipped: …`
  - means: missing _0/_1 partners were not checked this run
- `!! postflight weight-partner jiggle sync skipped: …`
  - means: the two weights of a pair may jiggle differently
- `!! POSTFLIGHT weight-partner parity: … shape(s) convert differently at _0 vs _1`
  - means: a per-weight metadata leak: both weights should morph identically; listed below
- `!! postflight weight-partner parity scan skipped: …`
  - means: _0/_1 parity was not checked this run
- `!! …`
  - means: the vertex-colour sweep fell back to running one file at a time and finished; nothing was lost
  - fix: if this repeats, lower the worker count in Settings
- `!! vertex-color sanitize failed: …`
  - means: vertex colours were left as the source had them
- `!! female-model restore failed: …`
  - means: continuing with male fallbacks
- `!! [unified] coverage empty/incomplete (ok=…, targets=…, body=…)`
  - means: merging per-source patches instead
- `!! … new ARMAs exceed the …-record ESL cap`
  - means: shipped as a NON-ESL full ESP (consumes one load-order slot)
  - fix: position it to win
- `!! could not remove stale …: …`
  - means: an old SkyPatcher ini may still apply beside the new one
  - fix: delete it by hand
- `!! the VANILLA SWEEP ran but linked 0 records`
  - means: vanilla armor no mod overrides will be invisible on UBE actors
  - fix: check the VANILLA SWEEP pass above for errors, or rerun just the sweep (Select mods -> 'vanilla')
- `!! alt-texture reconcile failed: …`
  - means: colour variants may bind to the wrong shape
  - fix: check the affected armour's variants in game
- `!! hands-slot fix failed: …`
  - means: hand pieces may keep their source slot
- `!! armature dedup failed: …`
  - means: duplicate armatures may remain in the Combined ESP
- `!! master re-sort failed: …`
  - means: the Combined ESP's masters may be out of order, and the game may refuse to load it
  - fix: check the merged plugin's master list in xEdit
- `!! POSTFLIGHT: … load-breaking + … other issue(s) on the FINAL Combined`
  - means: listed below; a load-breaking issue means the plugin is NOT safe to load
- `!! postflight validation skipped: …`
  - means: the plugin was not checked for load-breaking issues
- `!! auto-merge failed: …`
  - means: no Combined ESP this run; the per-source patches are still in the output
- `!! auto-merge SKIPPED: … mod(s) failed ESP generation`
  - means: the Combined ESP would otherwise be built from incomplete patches
  - fix: fix those mods (their errors are above) before merging
- `!! NO RACE COVERAGE GENERATED`
  - means: coverage is emitted as part of the merge, and the merge did not run (--no-auto-merge, a failed source, or no patches found); mod-defined helmets, circlets, jewelry and body variants will be INVISIBLE on UBE actors until a run completes the merge
  - fix: fix the failed source, or run again with the merge enabled
- `!! preview render failed: …`
  - means: no preview images this run; the conversion itself is unaffected
- `!! … NIF(s) reference a BODYTRI that doesn't exist on disk`
  - means: those pieces will not follow body sliders; listed below
- `!! BSA extract: refusing traversal path "…"`
  - means: the archive entry points outside the extraction folder and was skipped
- `!! vanilla sweep DISABLED this run: …`
  - means: vanilla armour that no mod overrides stays unlinked, so it is invisible on UBE actors until a run with the sweep
- `!! overlay transfer FAILED: …`
  - means: overlays were not transferred this run
- `!! UBE nude-skin morph check:`
  - means: listed below: a nude part without its morph file stays at base shape while the body morphs
  - fix: rebuild that part in BodySlide with 'Build Morphs' checked
- `!! … post-convert phase(s) FAILED`
  - means: see the errors above; the run is reported as failed
- `!! master re-sort failed: …`
  - means: the Combined ESP's masters may be out of order, and the game may refuse to load it
  - fix: check the merged plugin's master list in xEdit
- `!! POSTFLIGHT CTD on merged output: … load-breaking issue(s)`
  - means: NOT safe to load; listed below
- `!! postflight validation skipped: …`
  - means: the plugin was not checked for load-breaking issues
- `!! …`
  - means: … warning(s), listed below

## src/nif_convert_writer.py

- `!! … has … bones (> …-bone GPU cap) and could NOT be split into partitions -- in …`
  - means: may CTD on equip
- `!! … has … verts (> …-vert morph-rebuild cap) and could NOT be split into partitions -- in …`
  - means: may CTD on equip
- `!! partition normalize FAILED: … -- on …`
  - means: partitions left as-is, over-cap checks NOT run
- `!! re-author of … dropped shape(s) …`
  - means: kept the prior complete file (no partial commit)
