# Commits that changed formatting only. GitHub's blame view skips them; for
# local blame run once:  git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# None yet on this history. Add a full 40-character SHA per line.
