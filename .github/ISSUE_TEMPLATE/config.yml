blank_issues_enabled: true
contact_links:
  - name: Question, or not sure it's a bug? Start a discussion
    url: https://github.com/DayOnly/CBBEtoUBE-exe/discussions
    about: >-
      Setup trouble, "is this supposed to happen?", whether a modlist is
      supported, or showing off a conversion. Same report format as an issue --
      paste it in a code fence. Filing it here as an issue is fine too; it gets
      moved.
  - name: How to write a report we can act on
    url: https://github.com/DayOnly/CBBEtoUBE-exe/blob/main/REPORTING.md
    about: >-
      The report format, what to attach, and how to paste it into chat instead
      of filing here. The GUI's "Copy report" button fills in most of it for you.
  - name: Setup and dependency questions
    url: https://github.com/DayOnly/CBBEtoUBE-exe#dependencies
    about: >-
      Converted armor invisible? Overlay pass skipped? The Dependencies section
      lists every in-game prerequisite (SkyPatcher, RaceCompatibility, UBE,
      RaceMenu) and every optional tool (texconv, Papyrus compiler). Run "Check
      setup" in the GUI first — it verifies all of them.
  - name: How the refit works
    url: https://github.com/DayOnly/CBBEtoUBE-exe/blob/main/DESIGN.md
    about: >-
      Why a piece was fitted, skipped, or body-swapped the way it was. DESIGN.md
      documents the clearance passes, physics preservation, and delivery model.
